node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
server.js

# Credenciais Firebase — nunca versionar (prod e dev)
firebase-applet-config.json
firebase-applet-config.dev.json
service-account.json
service-account.dev.json

# Backup de estado local (gerado automaticamente em runtime)
data/

# Configurações locais do Claude Code
.claude/

